$.fn.lazyimg = function(offset) {

  offset = offset || 0;

  $(this).each(function() {
    
    var $img = $(this),
      windowHeight,
      scrollTop,
      imgTop;
    
    if(!$img.is(':visible')) {
      return;
    }
  });
};
